{
  "name": "@masatomakino/actions-pr-creator-check",
  "version": "0.1.20",
  "main": "index.js",
  "type": "module",
  "files": [
    "dist"
  ],
  "bin": {
    "release-helper": "dist/CLI.js"
  },
  "scripts": {
    "test": "echo \"no test specified\" && exit 0",
    "preversion": "node ./dist/cli.js --preversion",
    "postversion": "node ./dist/cli.js --postversion",
    "release": "node ./dist/cli.js --release",
    "dry-preversion": "node ./dist/cli.js --preversion --dry-run",
    "dry-postversion": "node ./dist/cli.js --postversion --dry-run",
    "dry-release": "node ./dist/cli.js --release --dry-run",
    "build": "tsc"
  },
  "author": "",
  "license": "UNLICENSED",
  "description": "",
  "dependencies": {
    "commander": "^12.1.0",
    "execa": "^9.5.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
