name: Deployment Experiment

on:
  workflow_dispatch:

jobs:
  gate-test:
    runs-on: ubuntu-22.04
    environment: npm-publish
    steps:
      - run: echo "Environment gate passed - this is a test"
